\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{dsfont}
\usepackage{cancel}
\usepackage[margin=1in]{geometry}
\usepackage[shortlabels]{enumitem}
\usepackage{tabto}
	\NumTabs{20}
\usepackage{nicefrac}
\usepackage[nobreak=true]{mdframed}
\usepackage{adjmulticol}
\usepackage{blkarray}
\usepackage[parfill]{parskip}
\usepackage[per-mode = symbol]{siunitx}
\usepackage{graphicx}
\usepackage{gensymb}
\usepackage{textcomp}
\usepackage{tabu}
\usepackage{xcolor}
\usepackage{listings, lstautogobble}
\usepackage{verbatim}
\usepackage{soul}

% pdf_tex figures
\usepackage{import}
\usepackage{xifthen}
\usepackage{pdfpages}
\usepackage{transparent}

\newcommand{\incfig}[2][0.9]{
    \def\svgwidth{#1\columnwidth}
    \import{./images/}{#2.pdf_tex}
}

\graphicspath{{./images/}}

\newcommand\horline{
    \vspace*{1em}
    \hrule
    \vspace*{0.5em}
}

\newcommand{\quotes}[1]{``#1''}

\newcommand{\skipabove}{\setlength{\abovedisplayskip}{0pt}}
\newcommand{\skipbelow}{\setlength{\belowdisplayskip}{0pt}}
\newcommand{\spaceabove}{\setlength{\abovedisplayskip}{10pt plus 2pt minus 5pt}}
\newcommand{\spacebelow}{\setlength{\belowdisplayskip}{10pt plus 2pt minus 5pt}}
\newcommand{\spacerightarrow}{\hspace*{0.6em} \longrightarrow \hspace*{0.6em}}

\lstset{ %
autogobble=true,
language=Python,                % choose the language of the code
basicstyle=\footnotesize,       % the size of the fonts that are used for the code
numbers=left,                   % where to put the line-numbers
numberstyle=\footnotesize,      % the size of the fonts that are used for the line-numbers
stepnumber=1,                   % the step between two line-numbers. If it is 1 each line will be numbered
numbersep=5pt,                  % how far the line-numbers are from the code
backgroundcolor=\color{white},  % choose the background color. You must add \usepackage{color}
showspaces=false,               % show spaces adding particular underscores
showstringspaces=false,         % underline spaces within strings
showtabs=false,                 % show tabs within strings adding particular underscores
frame=single,           % adds a frame around the code
tabsize=2,          % sets default tabsize to 2 spaces
captionpos=b,           % sets the caption-position to bottom
breaklines=true,        % sets automatic line breaking
breakatwhitespace=false,    % sets if automatic breaks should only happen at whitespace
escapeinside={\%*}{*)},          % if you want to add a comment within your code
xleftmargin={4em},
xrightmargin={4em},
literate={``}{\textquotedblleft}1,
mathescape=true
}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\newskip\saveskip
\newskip\saveindent
\saveskip=\baselineskip

\textheight=9in
\textwidth=6.5in
\topmargin=-.75in
\oddsidemargin=0.0in
\evensidemargin=0.0in
\parindent=0pt
\parskip=5pt
\itemsep=-1pt
\floatsep 9pt plus 2pt minus 3pt
\intextsep 9pt plus 2pt minus 3pt
\textfloatsep=9pt plus 2pt minus 3pt
\renewcommand{\baselinestretch}{1.0}
\font\dunhd=cmdunh10 scaled \magstep4
\font\dunhc=cmdunh10 scaled \magstep3
\font\dunhb=cmdunh10 scaled \magstep2
\font\dunha=cmdunh10 scaled \magstep1

\makeatletter
\def\maketitle{%
 \begingroup
 \newpage
 \global\@topnum\z@ \@maketitle
 \thispagestyle{myheadings}
 \endgroup
 \setcounter{footnote}{0}
 \let\maketitle\relax
 \let\@maketitle\relax}

\def\@maketitle{\newpage
\hrule height6pt \vskip .6em
\flushright
\makebox[1.5in][l]{\dunha \CourseNum}
{\dunha \CourseTit \hfill \Name \par}
\vspace{-4pt}
\flushright
\makebox[1.5in][l]{\dunha \Term}
{\dunha \Instructors \hfill \dunhc \WorkTit \ \WorkNumber \par}
\vskip 0.6em
\hrule height6pt
 \par
 \vskip .5em}

\def\section{\@startsection{section}{1}{\z@}{-8ex plus -.6ex minus -.2ex}{1.3ex plus .1ex}{\dunhb}}
\def\subsection{\@startsection{subsection}{2}{\z@}{-6ex plus -.5ex minus -.1ex}{0.9ex plus .1ex}{\dunha}}

%%%%%% page headings

\def\foottext{\begin{footnotesize}\Name\ --\ \SID \hfill\qquad\thepage\end{footnotesize}}

\def\ps@myheadings{\let\@mkboth\@gobbletwo
\def\@oddfoot{\foottext}%
\def\@evenfoot{\foottext}%
\def\@evenhead{}\def\@oddhead{}}
\makeatother

\pagestyle{myheadings}

%%%%% document-specific macros

% Under / Over Arrow Annotation
\newcommand{\underarrow}[3][0.6em]{
  \underset{
      \substack{\left\uparrow\vbox to #1{}\right.\\\hidewidth#2\hidewidth}
  }{#3}
}

\newcommand{\overarrow}[3][0.6em]{
  \overset{
      \substack{\hidewidth#2\hidewidth\\ \left\downarrow\vbox to #1{}\right.}
  }{#3}
}

% Theorems
\newtheoremstyle{boldnote}
  {1em} % Space above
  {\topsep} % Space below
  {\itshape} % Body font
  {} % Indent amount
  {\bfseries} % Theorem head font
  {.} % Punctuation after theorem head
  {.5em} % Space after theorem head
  {\thmname{#1}\thmnumber{ #2}\thmnote{ #3}} % Theorem head spec (can be left empty, meaning `normal')

\newtheoremstyle{nonitalic}
  {1em} % Space above
  {\topsep} % Space below
  {} % Body font
  {} % Indent amount
  {\bfseries} % Theorem head font
  {.} % Punctuation after theorem head
  {.5em} % Space after theorem head
  {\thmname{#1}\thmnumber{ #2}\thmnote{ #3}} % Theorem head spec (can be left empty, meaning `normal')

\theoremstyle{boldnote}
\newtheorem*{theorem}{Theorem}
\newtheorem*{corollary}{Corollary}
\newtheorem*{lemma}{Lemma}
\newtheorem*{definition}{Definition}
\theoremstyle{nonitalic}
\newtheorem*{remark}{Remark}
\renewcommand\qedsymbol{$\blacksquare$}

% NiceFrac
\newcommand\bignicefrac[2]{%
    \raise.5ex\hbox{$#1$}%
    \kern-.1em/\kern-.15em%
    \lower.25ex\hbox{$#2$}}

% Delimiters
\newcommand{\norm}[1]{\left\lVert#1\right\rVert}
\DeclarePairedDelimiter\ceil{\lceil}{\rceil}
\DeclarePairedDelimiter\floor{\lfloor}{\rfloor}
\newcommand{\abs}[1]{\left| #1 \right|}
\newcommand{\paren}[1]{\left( #1 \right)}
\newcommand{\set}[1] {\left\{ #1 \right\}}
\newcommand{\bracket}[1] {\left[ #1 \right]}
\newcommand{\angles}[1] {\langle #1 \rangle}
\newcommand{\suchthat}{\mathrel{\Big|}}

% Black Board Bold
\def\E{\mathbb{E}}
\def\N{\mathbb{N}}
\def\R{\mathbb{R}}
\def\C{\mathbb{C}}
\def\Q{\mathbb{Q}}
\def\Z{\mathbb{Z}}

% Misc Math Font Styles
\newcommand{\mbf}[1]{\mathbf{#1}}

% Real and Imaginary Parts to not use fraktur...
\renewcommand{\Re}{\operatorname{Re}}
\renewcommand{\Im}{\operatorname{Im}}

% Gaussian Elimination
\makeatletter
\renewcommand*\env@matrix[1][*\c@MaxMatrixCols c]{%
  \hskip -\arraycolsep
  \let\@ifnextchar\new@ifnextchar
  \array{#1}}
\makeatother

\newcount\arrowcount
\newcommand\arrows[1]{
        \global\arrowcount#1
        \ifnum\arrowcount>0
                \begin{matrix}[c]
                \expandafter\nextarrow
        \fi
}

\newcommand\nextarrow[1]{
        \global\advance\arrowcount-1
        \ifx\relax#1\relax\else \xrightarrow{#1}\fi
        \ifnum\arrowcount=0
                \end{matrix}
        \else
                \\
                \expandafter\nextarrow
        \fi
}

% Probability
\newcommand{\prob}[1]{\mathbb{P}\left( #1 \right)}
\newcommand{\expec}[1]{\mathbb{E}\left( #1 \right)}
\newcommand{\var}[1]{\operatorname{var}\left( #1 \right)}
\newcommand{\cov}[1]{\operatorname{cov}\left( #1 \right)}
\newcommand{\given}{\mathbin{\vert}}
\newcommand{\gaussian}{\mathcal{N}}
\newcommand{\indicator}{\mathds{1}}


% Algebra
\newcommand{\actson}{\mathrel{\rotatebox[origin=c]{-90}{$\circlearrowleft$}}}
\newcommand{\fundg}[1]{\pi_1\paren{#1}}
\newcommand{\rel}[1]{\mathrel{\paren{\operatorname{rel} #1}}}
